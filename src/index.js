import myFunction from "../src/App.js";
const first = document.getElementById("posicion_inicial").innerHTML = "Posicion Inicial:" + posicionInicial[0] + ',' + posicionInicial[1] + ' ' + orientacion;
const second = document.getElementById("comandos").innerHTML = "Comandos:" + direcciones;
const third = document.getElementById("posicion_final").innerHTML = "Posicion Final:" + posicionFinal[0] + ',' + posicionFinal[1] + ' ' + orientacionFinal;


form.addEventListener("submit", (event) => {
    event.preventDefault();

});